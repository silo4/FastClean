#This is Data Layer.

##There are two data source, one is from local cache(file cache, memory cache, db cache, etc) and the other is from cloud(or remote process, service). So we should deal with the two aspects. 